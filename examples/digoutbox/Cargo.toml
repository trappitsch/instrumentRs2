[package]
name = "digoutbox"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
instrumentrs = { workspace = true }

[dev-dependencies]
instrumentrs = { workspace = true, features = ["mock-interface"] }
serialport = "4.9.0"
