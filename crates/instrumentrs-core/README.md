# instrumentrs-core

This crate serves the core of the `instrumentRs` toolkit.
All modules with names starting with `__`
are not considered part of the public API.
These modules are reexported in `instrumentRs`
as modules starting with `__` as well.
